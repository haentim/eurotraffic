.venv/
__pycache__/
*.pyc
.shiny/

# Source dataset — cloned separately via scripts/fetch_data_repo.sh
traffic-volume-data-EU-cities/

# Local preprocessing artifacts (regenerable; too large to track)
/data/
/cache/

# Standalone web app build output (built in CI from web/data/)
site/
web/app/cities.json

# Working notes — local only, not tracked
SESSION_HANDOFF.md
WEB_APP_PLAN.md
DENSITY_MODEL_MATH.md

# NOTE: web/data/*.sqlite.gz (the slim per-city files, ~76 MB) ARE committed —
# GitHub Pages serves them and the CI build copies them into site/data.
